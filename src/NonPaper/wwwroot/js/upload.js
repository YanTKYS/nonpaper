import { api, eventId, token, showError } from './common.js';
let eventData; back.href=`/manage.html?event=${encodeURIComponent(eventId)}&token=${encodeURIComponent(token)}`;
async function load(){try{eventData=await api(`/api/events/${eventId}/manage`,{},true);render();}catch(e){showError(e);}}
function render(){documents.replaceChildren();const sorted=[...eventData.documents].sort((a,b)=>a.order-b.order);sorted.forEach((doc,index)=>{const row=document.createElement('div');row.className='document';const name=document.createElement('strong');name.textContent=doc.title;row.append(name);[['名称変更',()=>rename(doc)],['↑',()=>move(index,-1)],['↓',()=>move(index,1)],['削除',()=>remove(doc)]].forEach(([label,fn])=>{const b=document.createElement('button');b.textContent=label;b.addEventListener('click',fn);row.append(b)});documents.append(row)});if(!sorted.length)documents.textContent='資料はまだありません。';}
async function rename(doc){const value=prompt('新しい資料表示名',doc.title);if(!value)return;try{await api(`/api/events/${eventId}/documents/${doc.id}`,{method:'PUT',body:JSON.stringify({title:value})},true);await load();}catch(e){showError(e);}}
async function move(index,delta){const sorted=[...eventData.documents].sort((a,b)=>a.order-b.order);const target=index+delta;if(target<0||target>=sorted.length)return;[sorted[index],sorted[target]]=[sorted[target],sorted[index]];try{await api(`/api/events/${eventId}/documents/order`,{method:'PUT',body:JSON.stringify({documentIds:sorted.map(x=>x.id)})},true);await load();}catch(e){showError(e);}}
async function remove(doc){if(!confirm(`資料「${doc.title}」を削除しますか？`))return;try{await api(`/api/events/${eventId}/documents/${doc.id}`,{method:'DELETE'},true);await load();}catch(e){showError(e);}}
async function upload(list){if(!list.length)return;const form=new FormData();[...list].forEach(x=>form.append('files',x));progress.textContent='アップロード処理中です…';try{await api(`/api/events/${eventId}/documents`,{method:'POST',body:form},true);files.value='';progress.textContent='アップロードが完了しました。';await load();}catch(e){progress.textContent='';showError(e);}}
send.addEventListener('click',()=>upload(files.files));drop.addEventListener('dragover',e=>e.preventDefault());drop.addEventListener('drop',e=>{e.preventDefault();upload(e.dataTransfer.files)});load();
